{
  "name": "new-tab",
  "version": "1.0.0",
  "description": "A beautiful, minimal custom browser new tab page with dynamic wallpapers",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "node generate-wallpapers.js",
    "prebuild": "node generate-wallpapers.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "new-tab",
    "browser",
    "wallpapers"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "compression": "^1.8.1",
    "express": "^4.18.2",
    "multer": "^2.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
